name: Build SAD EXE

on:
  workflow_dispatch:         # ⬅️ Дозволяє запуск вручну
  release:
    types: [created]         # ⬅️ Запускається при створенні релізу

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: 🔄 Клонування репозиторію
      uses: actions/checkout@v3

    - name: 🐍 Встановлення Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 Встановлення залежностей
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    - name: 🛠️ Створення EXE з іконкою
      run: |
        pyinstaller --onefile --icon=sad_icon.ico sad_app.py

    - name: 📤 Завантаження EXE до релізу
      if: github.event_name == 'release'
      uses: softprops/action-gh-release@v1
      with:
        files: dist/sad_app.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
